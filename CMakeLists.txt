cmake_minimum_required(VERSION 3.10)
project(ShortestOthello C)

add_executable(othello main.c)
if(MSVC)
    target_compile_options(othello PRIVATE /w /FIstdio.h)
    target_compile_definitions(othello PRIVATE _CRT_SECURE_NO_WARNINGS)
else()
    target_compile_options(othello PRIVATE -w)
endif()
